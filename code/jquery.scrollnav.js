(function($) {

    $.fn.scrollnav = function(parameters) {
        var
        default = {

        };
        var options = $.extend({}, default, parameters);

        return this.each(function() {

        });
    };

})(jQuery);
